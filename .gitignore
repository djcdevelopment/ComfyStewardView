### workspace artifacts ###
.tools/
# ignore exported world data that we will keep locally
*.db
*.fwl*

# Daemon runtime logs â€” re-generated on every restart
viewer/logs/*.log
viewer/logs/*.err

# Maven build output. Tracking it made every branch that compiled conflict with
# every other one in .class files and 89 MB jars, which is what blocked PR #3 —
# no source file disagreed. It also covers the batch analytics artifacts
# (*.duckdb, rendered-*/, generation-*.log) that used to be listed one by one.
viewer/target/
design_mocks/.thumbnail
/steward-config.json
# Synthetic validation saves, caches, ledgers, PNGs, and receipts
/artifacts/synthetic-history/
